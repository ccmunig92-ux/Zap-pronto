import assert from "node:assert/strict";import test from "node:test";
import { evaluateCapacityAlertTarget,runCapacityAlertWorker,type CapacityAlertEvaluationTarget } from "./capacity-alert-runner.js";
const target:CapacityAlertEvaluationTarget={tenantId:"10000000-0000-4000-8000-000000000001",unitId:"20000000-0000-4000-8000-000000000001"};
function pool(){const calls:{text:string;values?:unknown[]}[]=[];const client={async query(text:string,values?:unknown[]){calls.push({text,...(values?{values}:{})});return{rows:[]}},release(){}};return{calls,pool:{async connect(){return client},async end(){}}};}
test("capacity evaluator installs tenant context and only calls the lifecycle function",async()=>{const mock=pool();await evaluateCapacityAlertTarget(mock.pool,target,new Date("2026-08-23T12:00:00Z"));assert.equal(mock.calls[2]?.text,"SELECT set_config('app.tenant_id',$1,true)");assert.deepEqual(mock.calls[2]?.values,[target.tenantId]);assert.match(mock.calls[3]?.text??"",/evaluate_unit_capacity_alert_episode/);assert.deepEqual(mock.calls[3]?.values,[target.unitId,"2026-08-23T12:00:00.000Z"]);assert.equal(mock.calls.some(call=>/outbound|http|fetch/i.test(call.text)),false)});
test("capacity evaluator rejects malformed tenant or unit targets before opening SQL",async()=>{const mock=pool();await assert.rejects(evaluateCapacityAlertTarget(mock.pool,{...target,tenantId:"bad"}),/CAPACITY_ALERT_TARGET_INVALID/);assert.equal(mock.calls.length,0)});
test("capacity worker stops during idle delay",async()=>{const mock=pool(),controller=new AbortController();const running=runCapacityAlertWorker(mock.pool,{pollIntervalMs:60_000,targets:[target]},controller.signal);await new Promise(resolve=>setImmediate(resolve));controller.abort();await running;assert.equal(mock.calls.filter(call=>call.text.includes("evaluate_unit_capacity_alert_episode")).length,1)});
