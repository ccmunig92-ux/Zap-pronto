import assert from"node:assert/strict";import test from"node:test";import{createApiClient}from"./client.js";
test("operational timezone client uses unit path and stable command key",async()=>{const requests:Request[]=[];const unitId="33333333-3333-4333-8333-333333333333",base={unitId,timeZone:"America/Sao_Paulo",version:1,updatedAt:"2026-08-13T12:00:00.000Z"};const client=createApiClient({baseUrl:"https://api.example.test",getAccessToken:async()=>"token",fetch:async request=>{requests.push(request);return Response.json(request.method==="GET"?base:{...base,version:2,replayed:false})}});assert.equal((await client.getUnitOperationalTimezone(unitId)).timeZone,"America/Sao_Paulo");await client.setUnitOperationalTimezone(unitId,{expectedVersion:1,timeZone:"America/Manaus"},"timezone-command-1");assert.equal(new URL(requests[0]!.url).pathname,`/v1/units/${unitId}/operational-timezone`);assert.equal(requests[1]!.headers.get("idempotency-key"),"timezone-command-1");assert.deepEqual(await requests[1]!.json(),{expectedVersion:1,timeZone:"America/Manaus"})});
